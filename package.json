{
  "name": "neural-link-monorepo",
  "version": "5.0.0",
  "description": "Neural-Link Brain-AI Hybrid Interface Simulator",
  "scripts": {
    "dev": "npm run dev:all",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "cd backend && python startup_check.py && uvicorn main:app --reload --port 8000 --log-level warning",
    "dev:all": "concurrently --names BACKEND,FRONTEND --prefix-colors cyan,magenta \"npm run dev:backend\" \"npm run dev:frontend\"",
    "check": "cd backend && python startup_check.py",
    "install:backend": "cd backend && pip install -r requirements.txt",
    "install:frontend": "npm install --prefix frontend",
    "install:all": "npm install && npm run install:frontend && npm run install:backend"
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
